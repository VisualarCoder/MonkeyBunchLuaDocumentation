## Set Player Properties - Networking API Reference
```lua
-- Creates a key or sets an already made key inside of the local player's properties
local key = "key"
local value = "value"
NetworkingAPI.SetPlayerProperties(key, value)
```